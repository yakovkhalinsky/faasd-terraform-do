# don't commit tfvars files they contain sensitive information
# such as your DO API token
environments/*.tfvars

# terraform plugins and providers
.terraform

# terraform lock file, generated by 'terraform init'
.terraform.lock.hcl
